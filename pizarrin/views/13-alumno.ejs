<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nuestra Pizarra Más Popular</title>
    <link rel="stylesheet" href="/stylesheets/pizarrin.css">
    <style>
        .notifications {
            position: fixed;
            top: 10px;
            right: 10px;
            width: 300px;
            max-height: 400px;
            overflow-y: auto;
            z-index: 1000;
        }
        .notification {
            background-color: #f8d7da;
            color: #721c24;
            padding: 10px;
            margin: 5px 0;
            border: 1px solid #f5c6cb;
            border-radius: 5px;
            font-size: 14px;
        }
    </style>
</head>
<body>
<div class="browser-header">
    <div class="nav-links">
        <div class="dropdown">
            <a href="#" class="dropdown-toggle"><%= email %></a>
            <div class="dropdown-menu">
                <button onclick="window.location.href='/logout'">Logout</button>
                <button onclick="window.location.href='/cambiar_contrasena'">Cambiar contraseña</button>
            </div>
        </div>
    </div>
    <div class="profile-icon">👤</div>
</div>

<!-- Main Content -->
<div class="content2">
    <h1><%= carrera %></h1>
    <h2>Aulas disponibles</h2>
    <div class="examples">
        <% if (aulas.length > 0) { %>
            <% aulas.forEach(aula => { %>
                <button class="example" onclick="window.location.href='/aula/<%= aula.id %>'">
                    <img src="/images/aguacate.png" alt="<%= aula.name %>" width="100" height="100">
                    <h2><%= aula.name %></h2>
                </button>
            <% }) %>
        <% } else { %>
            <p>No hay aulas disponibles para este alumno.</p>
        <% } %>
    </div>
</div>

</body>
</html>
