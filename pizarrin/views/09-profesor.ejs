<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nuestra Pizarra Más Popular</title>
    <link rel="stylesheet" href="/stylesheets/pizarrin.css">
</head>
<body>
<div class="browser-header">
    <div class="menu-icon">&#9776;</div>
    <div class="search-container">
        <input type="text" placeholder="Search">
        <button>🔍</button>
    </div>
    <div class="nav-links">
        <div class="dropdown">
            <a href="#" class="dropdown-toggle"><%= email %></a>
            <div class="dropdown-menu">
                <button onclick="window.location.href='/logout'">Logout</button>
            </div>
        </div>
    </div>
    <div class="profile-icon">👤</div>
</div>

<div>
    <button class="solicitar-cuenta botones_admin" onclick="window.location.href='/solicitar_cuenta_alumno'">Solicitar cuenta alumno</button>
    <button class="botones_admin" id="crear-aula" onclick="window.location.href='/crear_aula'">Crear aula</button>
    <!-- <button class="botones_admin" id="crear-aula-publica" onclick="window.location.href='/crear_aula_publica'">Crear aula pública</button> -->
</div>

<!-- Main Content -->
<!-- Main Content -->
<div class="content2">
    <h1><%= carrera %></h1>
    <h2>Aulas disponibles</h2>
    <div class="examples">
        <% if (aulas && aulas.length > 0) { %>
            <% aulas.forEach(aula => { %>
                <div class="example">
                    <button onclick="window.location.href='/aula/<%= aula.id %>'">
                        <img src="/images/aguacate.png" alt="<%= aula.name %>" width="100" height="100">
                        <h2><%= aula.name %></h2>
                    </button>
                    <button onclick="window.location.href='/aula/<%= aula.id %>/editar'">
                        Editar
                    </button>
                </div>
            <% }) %>
        <% } else { %>
            <p>No tienes aulas creadas.</p>
        <% } %>
    </div>
    
    </div>
</div>



</body>
</html>
